export const appRoutes = {
  dashboard: "/",
  calendar: "/calendar",
  tasks: "/tasks",
  timetable: "/timetable",
  studyTimer: "/study/timer",
  studyLogs: "/study/logs",
  grades: "/grades",
  analytics: "/analytics",
  settings: "/settings"
} as const;
