# Schema

This directory is reserved for schema snapshots, exported DDL, and future ER diagrams.

Current source of truth:

- `../migrations/`: ordered raw SQL migrations
- `../README.md`: operational documentation for setup, commands, and relationships
